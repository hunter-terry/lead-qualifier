# docs

[`walkthrough.html`](walkthrough.html) is a plain-language, client-facing
demo page — two real runs against the live workflow (a qualified hot
lead, and a blank/broken lead that the AI still confidently scored
until validation caught it), no code shown. Built 2026-07-11, published
as a shareable Claude Artifact (see `job-search/upwork-profile.md` in
the vault root for the live link). This file is the saved source; open
it in a browser to view directly.
